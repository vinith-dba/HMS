import { InvoiceSheet } from "@/components/print/invoice-sheet";

/** The Jeeva bill — one shared Bill-of-Supply sheet, printable from this portal. */
export default function InvoicePrintPage() {
  return <InvoiceSheet />;
}
